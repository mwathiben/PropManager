import { readFileSync, writeFileSync } from 'node:fs';

const path = 'phase-102-audit-prd.json';
const prd = JSON.parse(readFileSync(path, 'utf8'));

for (const finding of prd.findings) {
    finding.passes = true;
}

prd.closeout = {
    closed_at: '2026-05-24',
    result: '3/3 findings pass — zero deferrals. Phase 2 of the owner / PM-company model: the owner login + a strictly-scoped read-only portal, building on the Phase-101 owner contact.',
    summary:
        "Gives a Phase-101 PropertyOwner contact a LOGIN. An invite-only `owner` role mirroring water_client (users.role plain string; invitations.role + onboarding_sessions.role ENUMs widened). OwnerInvitationController: landlord invites an owner contact (404 foreign / 422 already-has-login / no-email / email-taken / 30-day already-pending guards) → queued OwnerInvitation mail (markdown + failed() handler) → public deep-link accept mints the owner User (role+landlord_id set explicitly, email_verified_at=now) and claims property_owners.user_id via withoutGlobalScope('landlord')->where(landlord_id)->whereNull(user_id)->update with a 0-row-throw inside DB::transaction (claim-race rolls the mint back), then logRoleChange + login + redirect to the portal. Self-register blocked: RegisteredUserController abort_if role in [water_client, owner] (one-time token NOT burned) + GET /register?invitation=<owner-token> redirects to the deep-link. Portal: ResolvesCurrentOwner trait is the single scope source (abort_unless isOwner 403, then PropertyOwner where user_id + landlord_id firstOrFail); owner-portal.* group ['auth','verified','role:owner']; OwnerPortalDashboardController shows the owner's own properties' metrics; OwnerPortalStatementsController index/download reuse OwnerStatementService::forOwner with the owner's OWN auth-derived ids. DashboardController routes an owner into the portal; AuthenticatedLayout owner nav + role badge. Vue Owner/{Dashboard,Statements,AcceptInvitation}. Lang en/sw/ar.",
    review: 'Full 3-agent multi-reviewer pass (CodeRabbit + pr-review code-reviewer + silent-failure-hunter) — all three executed for real this time (each cited specific line numbers; 34-57 tool-uses; no quota-empty returns, the failure mode the user flagged in Phase 101). Convergent verdict: NO CRITICAL/HIGH isolation defects — the three security-priority areas all confirmed correct + test-covered: (a) owner/tenant isolation (every portal query bound to BOTH property_owner_id AND landlord_id from auth via ResolvesCurrentOwner, never a route param; sibling-owner-under-same-landlord negative case tested for both dashboard and statement), (b) invite-only self-register gate (abort_if before User::create, token not consumed on rejection — tested), (c) claim-race rollback (0-row update throws inside the transaction, minted user rolls back — tested). FIXED from the silent-failure hunt: HIGH the OwnerInvitation mailable lacked a failed() handler (a dropped send left the owner with no link AND a 30-day re-invite block, invisibly) → added, mirroring OwnerStatementMail; MEDIUM the portal statement endpoints ignored forOwner()\'s documented null contract → abort_if($data===null,404) on both index + download; MEDIUM a crafted ?period=custom (no owner date UI) fell through getReportDateRange to subMonths(0) = "this month so far", silently understating the statement → dropped \'custom\' from the owner-portal whitelist so it normalizes to 12 months (regression-tested). LOW the duplicated currency lookup → resolveCurrency() helper. Accepted as-is: the dashboard\'s per-property PropertyMetricsService::forProperty N×4 query pattern (typical owner portfolios are small; throttle-protected) and the Statements.vue hardcoded period labels (verbatim from the reused ReportsTab.vue — codebase precedent, not new drift).',
    tests: 'Phase102OwnerPortalTest 19 (invite happy-path + no-email + already-has-login[422] + foreign-landlord[403/404] + duplicate-pending + email-already-a-user; self-register-blocked[403, token intact] + register-redirects-to-deep-link; accept-creates-linked-login + used-invitation + removed-contact + claim-race-no-overwrite; owner→portal redirect; SCOPING ISOLATION: dashboard shows only own properties + statement aggregates only own properties [sibling owner under same landlord invisible]; unknown/custom period → 12mo not this-month; statement PDF download; non-owner roles 403; guest→login). Sibling regression GREEN: Phase101OwnerFoundation 10 + Phase95WaterClientOnboarding + Phase94WaterClientsFoundation + Auth/RegistrationTest = 35 passed/101 assertions (the self-register guard widening to [water_client, owner] did not break registration or the water flow). Gates: Pint 21 files, eslint 0 errors, nav-audit clean (owner-invite.show allowlisted like water-invite.show), check-import-case + check-import-exists 441 files OK, vite build exit 0, lang parity en/sw/ar (owners.php 60 keys each + role.owner in *.json + emails.subjects.owner_invitation). New migration → dev DB migrated.',
    constraints_preserved:
        'Owner login is INVITE-ONLY (never self-registerable; both the role gate and the deep-link redirect enforce it). Portal scope is ALWAYS derived from auth (ResolvesCurrentOwner: user_id + landlord_id), never from a route param — an owner can never see another owner\'s or the PM\'s other data, even in the TenantScope boot-order window (controllers also bind landlord_id explicitly). The owner role is additive and mirrors water_client (no migration for users.role; ENUMs widened reversibly). The portal is READ-ONLY (no mutations). forOwner reuse keeps cash-basis, voided + water-client excluded. Deferred to later owner phases: owner payouts/disbursements, %-share / multi-owner-per-property, owner-side document sharing, and the dashboard metrics N+1 batching.',
};

writeFileSync(path, JSON.stringify(prd, null, 2) + '\n');
console.log('closeout written:', prd.findings.length, 'findings set passes:true');
